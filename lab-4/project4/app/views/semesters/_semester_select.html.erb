<%# required locals: 
    + semester_list (a list of all the semesters, in the format: [[description, code],[...]]) 
    + submit_text (text that appears over the submit button) 
    + path (path to the contoller method)
    + method (HTTP Request method) %>
    
<div>
  <%= form_for :selection, url: path, method: method do |form| %>
  <%= form.label :semester %>
  <%= form.select :semester, semester_list%>
  <%= form.submit submit_text %>
<% end %>
</div>